SECRET_KEY=secret
# Judge integration. Leave JUDGE_SECRET empty to use mock judging (dev/CI).
JUDGE_SECRET=
JUDGE_URL=https://judge.ksp.sk
# Namespace used for all submissions. Falls back to Competition.judge_namespace
# if empty.
JUDGE_NAMESPACE=
# USE_MOCK_JUDGE defaults to true when JUDGE_SECRET is empty.
